function setCount(value) {
  const count = document.getElementById('pomodoro_count');
  count.value = value;
}

function setText(text) {
  const submit = document.getElementById('submit');
  submit.value = text;
}

document.getElementById('counter').innerHTML++;

if (Number(document.getElementById('counter').innerHTML, 10) > 10)
  setText(
    `Il massimo dei giocatori è 10`
  );
else
  setText(
    `Gioca con ${document.getElementById('counter').innerHTML} giocatori`
  );

document.addEventListener(
  'onSubmit',
  setCount(document.getElementById('counter').innerHTML)
);
